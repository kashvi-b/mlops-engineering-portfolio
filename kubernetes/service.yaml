apiVersion: v1
kind: Service
metadata:
  name: spam-api
  labels:
    app: spam-api
spec:
  type: NodePort
  selector:
    app: spam-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
